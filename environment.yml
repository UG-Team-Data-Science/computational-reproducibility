name: vscode-binder
channels:
  - conda-forge
dependencies:
  - numpy
  - python-build
  - jupyter-server-proxy    
  - sphinx
  - python-dotenv
  - code-server >=3.2
  - pip:
      - sphinx-revealjs
      - esbonio
      - sphinx-autobuild
