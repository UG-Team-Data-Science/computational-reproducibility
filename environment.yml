name: vscode-binder
channels:
  - conda-forge
dependencies:
  - numpy
  - python-build
  - jupyter-server-proxy    
  - sphinx
  - python-dotenv
  - code-server=4.104
  - pip:
      - sphinx-revealjs
      - esbonio
      - sphinx-autobuild
      - notebook-intelligence
